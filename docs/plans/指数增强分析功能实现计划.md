# 指数增强分析功能实现计划

## 一、需求概述

**目标**：开发指数增强分析功能，分析如何通过持有指定指数/ETF的成分股跑赢指数（获取超额收益Alpha）。

**数据来源**：SQLite数据库中创建成分股表，插入模拟数据（待办：后续接入真实指数成分股数据）。

**分析维度**：基础维度 + 进阶维度 + 补充维度（共10个维度，30+指标）。

---

## 二、当前状态分析

- 已有 `index_daily` 表存储指数日频数据
- 已有 `stock_daily` 表存储股票日频数据
- 已有因子评估和回测框架
- **缺失**：指数成分股管理、相对收益分析功能

---

## 三、新增数据库表

### 3.1 index_constituent（指数成分股表）

```sql
CREATE TABLE IF NOT EXISTS index_constituent (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    trade_date DATE NOT NULL,
    index_code VARCHAR(20) NOT NULL,
    stock_code VARCHAR(20) NOT NULL,
    weight REAL NOT NULL,
    market_cap REAL,
    pe_ratio REAL,
    pb_ratio REAL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(trade_date, index_code, stock_code)
);
```

### 3.2 portfolio_analysis（组合分析结果表）

```sql
CREATE TABLE IF NOT EXISTS portfolio_analysis (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    analysis_date DATE NOT NULL,
    portfolio_id VARCHAR(50) NOT NULL,
    benchmark_code VARCHAR(20),
    portfolio_return REAL,
    benchmark_return REAL,
    excess_return REAL,
    tracking_error REAL,
    information_ratio REAL,
    beta REAL,
    alpha REAL,
    max_drawdown REAL,
    max_relative_drawdown REAL,
    attribution_data TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(analysis_date, portfolio_id)
);
```

### 3.3 factor_exposure（因子暴露表）

```sql
CREATE TABLE IF NOT EXISTS factor_exposure (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    trade_date DATE NOT NULL,
    portfolio_id VARCHAR(50) NOT NULL,
    factor_name VARCHAR(50) NOT NULL,
    portfolio_exposure REAL,
    benchmark_exposure REAL,
    active_exposure REAL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(trade_date, portfolio_id, factor_name)
);
```

---

## 四、10个分析维度

### 基础维度
1. **收益分析**：超额收益、累计Alpha、胜率、盈亏比
2. **行业偏离分析**：行业偏离度、行业集中度、行业配置贡献
3. **市值偏离分析**：市值暴露、市值分层暴露、市值风格贡献
4. **个股偏离分析**：个股权重偏离、主动权重、个股选择贡献

### 进阶维度
5. **因子暴露分析**：价值/成长/动量/波动/质量因子暴露
6. **跟踪误差与信息比率**：TE、IR、调整IR
7. **Beta与Alpha分析**：Beta、Alpha、下行Beta、时变Beta

### 补充维度
8. **盈利能力分析**：ROE/ROA/毛利率/净利率暴露、盈利稳定性
9. **风险调整后收益**：夏普/索提诺/卡玛/特雷诺/Omega比率
10. **回撤与尾部风险**：最大回撤、相对回撤、VaR、CVaR、偏度、峰度

---

## 五、文件变更计划

### 新建文件

| 文件路径 | 说明 |
|---------|------|
| `src/enhancement/__init__.py` | 模块初始化 |
| `src/enhancement/metrics.py` | 指标计算工具（30+指标） |
| `src/enhancement/attribution.py` | Brinson归因分析 |
| `src/enhancement/analyzer.py` | 核心分析器（整合10个维度） |
| `src/enhancement/data_generator.py` | 成分股模拟数据生成 |
| `src/enhancement/report.py` | 分析报告生成 |

### 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `src/data/database.py` | 新增3个表的创建和CRUD方法 |
| `src/factors/execution_logger.py` | 新增指数增强分析日志记录 |
| `config/config.py` | 新增指数增强配置 |
| `main.py` | 新增指数增强分析流程 |

---

## 六、核心类设计

### 6.1 IndexEnhancementAnalyzer（核心分析器）

```python
class IndexEnhancementAnalyzer:
    def __init__(self, db_manager, benchmark_code='000300.SH', execution_logger=None)
    def analyze(self, portfolio_weights, start_date, end_date, portfolio_id) -> Dict
    def _analyze_returns(self, ...) -> Dict
    def _analyze_industry(self, ...) -> Dict
    def _analyze_market_cap(self, ...) -> Dict
    def _analyze_factor_exposure(self, ...) -> Dict
    def _calculate_tracking_metrics(self, ...) -> Dict
```

### 6.2 AttributionAnalyzer（归因分析器）

```python
class AttributionAnalyzer:
    def brinson_attribution(self, portfolio_weights, benchmark_weights, ...) -> Dict
    def factor_attribution(self, portfolio_returns, factor_returns, ...) -> Dict
```

### 6.3 MetricsCalculator（指标计算器）

```python
class MetricsCalculator:
    @staticmethod
    def calculate_excess_return(portfolio_returns, benchmark_returns) -> pd.Series
    def calculate_tracking_error(excess_returns) -> float
    def calculate_information_ratio(excess_returns, tracking_error) -> float
    def calculate_beta(portfolio_returns, benchmark_returns) -> float
    def calculate_alpha(portfolio_returns, benchmark_returns, risk_free_rate) -> float
```

---

## 七、实施步骤

1. **数据库层**：在database.py中新增3个表的创建和CRUD方法
2. **指标计算**：创建metrics.py实现30+指标计算
3. **归因分析**：创建attribution.py实现Brinson归因
4. **核心分析器**：创建analyzer.py整合10个维度
5. **数据生成**：创建data_generator.py生成模拟成分股数据
6. **日志集成**：扩展execution_logger.py支持增强分析记录
7. **主程序集成**：在main.py中新增分析流程
8. **配置更新**：更新config.py
9. **测试验证**：编写测试用例

---

## 八、待办事项

- [ ] 接入真实指数成分股数据源（如聚宽、Tushare等）
- [ ] 支持更多基准指数（中证500、中证1000等）
- [ ] 可视化报告（HTML/PDF）
- [ ] 实时监控功能

---

## 九、预期输出

```
[指数增强分析]
✓ 超额收益: 6.87%
✓ 跟踪误差: 4.52%
✓ 信息比率: 1.52
✓ Beta系数: 0.98
✓ Alpha系数: 6.23%

[Brinson归因]
配置效应: +1.23%
选择效应: +4.85%
交互效应: +0.79%

[因子暴露]
价值因子: -0.35
成长因子: +0.42
动量因子: +0.18
```
