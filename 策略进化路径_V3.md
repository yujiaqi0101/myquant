 # 策略进化路径
 
 ## 基本信息
 
 | 项目 | 值 |
 |------|-----|
 | 策略名称 | 小市值优化策略 |
 | 东财策略ID | `f3e369e3-60e4-11f1-a22a-30c59923a9df` |
 | 当前版本 | V3 |
 
 ## V1 (初始版本)
 
 多因子选股 + 月度调仓（两步调仓：T日卖出 → T+1日买入）
 
 ## V2 (异动止盈)
 
 V2.1 卖出部成修复: 重试时查询实际持仓量
 V2.2 异动检测 + 移动止盈
 
 ## V3 (无状态改造 - 2026-06-11)
 
 ### 问题背景
 
 东财掘金中 `context` 对象在进程重启后全部丢失。V3 之前大量依赖 context 变量跨日保持状态：
 
 | 丢失变量 | 后果 |
 |----------|------|
 | `_pending_buy` | 买入阶段永远不执行 |
 | `_rebalance_month` | 每月多次调仓 |
 | `_pending_retry_buys/sells` | 重试失败 |
 | `_entry_info/_surge_peaks/_surge_mode` | 异动止盈完全失效 |
 
 ### 改动内容
 
 1. **单阶段调仓**: 将两阶段调仓改为"选股→卖出→买入"同日完成，消除 `_pending_buy` 跨日依赖
 2. **月份判定**: 用官方 `get_previous_n_trading_dates()` 替代 `_rebalance_month`
 3. **异动止盈无状态化**: 每次从价格数据重算峰值和回撤，不再依赖持久化状态
 4. **重试机制简化**: 月初非首日跳过重试，未成交订单下月调仓自动处理
 5. **on_order_status 简化**: 移除重试队列维护，仅保留日志
 
 ### 关键发现
 
 `get_previous_n_trading_dates(exchange='SHSE', date=today_str, n=1)` 是 gm SDK 官方函数（源文件 `gm/api/ds_instrument.py`），2f0e5b5a 掘金样例代码中已使用。该函数需 token 认证，只能在策略运行时调用。
 
 ### 改造后文件结构
 
 移除的 context 变量: `rebalance_count`, `target_weights`, `_rebalance_month`, `_pending_buy`,
 `_pending_retry_buys/sells`, `_max_retry_days`, `_entry_info`, `_surge_peaks`, `_surge_mode`
 
 保留的 context 变量: `risk_mode`（仅用于 `_calc_position_pct` 的回撤恢复判断）
 
 ### 验证
 
 1个月回测: 6.18% 收益, 6.7秒完成，无错误，无超时
